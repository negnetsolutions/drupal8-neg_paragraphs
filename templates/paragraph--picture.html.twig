{% if mobile|length > 0 %}
  <figure {{ attributes }}>
  {% if link %}
  <a href="{{link}}">
  {% endif %}
  <div class="rs-image-wrapper">
    <picture>
      {% if lazyload %}
      <source sizes="{{ image.sizes }}" {% if image.srcset|length > 0 %}data-srcset="{{ image.srcset }}"{% else %}src="{{ image.src }}"{% endif %} media="(min-width: 800px)" />
      <img decoding="async" src="data:image/gif;base64,R0lGODlhAQABAAAAACH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" width="{{ mobile.width }}" height="{{ mobile.height }}" sizes="{{ mobile.sizes }}" {% if mobile.srcset|length > 0 %}data-src="{{ mobile.src }}" data-srcset="{{ mobile.srcset }}"{% else %}data-src="{{ mobile.src }}"{%endif %} class="{{image.class}}" alt="{{alt}} "/>
      {% else %}
      <source sizes="{{ image.sizes }}" {% if image.srcset|length > 0 %}srcset="{{ image.srcset }}"{% else %}src="{{ image.src }}"{% endif %} media="(min-width: 800px)" />
      <img decoding="async" width="{{ mobile.width }}" height="{{ mobile.height }}" sizes="{{ mobile.sizes }}" {% if mobile.srcset|length > 0 %}src="{{ mobile.src }}" srcset="{{ mobile.srcset }}"{% else %}src="{{ mobile.src }}"{% endif %} class="{{image.class}}" alt="{{alt}} "/>
      {% endif %}
    </picture>
  </div>
  {% if captions|length > 0 %}
  {% for caption in captions %}
    <figcaption>
      <div class='textbox'>
        {{ caption }}
      </div>
    </figcaption>
  {% endfor %}
  {% endif %}
  {% if htmlAppend %}
    {{ htmlAppend|raw }}
  {% endif %}
  {% if link %}
  </a>
  {% endif %}
</figure>
{% else %}
  <figure {{ attributes }}>
    {% if link %}
    <a href="{{link}}">
    {% endif %}
    <div class="rs-image-wrapper">
      {% if lazyload %}
      <img decoding="async" src="data:image/gif;base64,R0lGODlhAQABAAAAACH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==" width="{{ image.width }}" height="{{ image.height }}" sizes="{{ image.sizes }}" {% if image.srcset|length > 0 %}data-src="{{ image.src }}" data-srcset="{{ image.srcset }}"{% else %}src="{{ image.src }}"{% endif %} class="{{image.class}}" alt="{{alt}} "/>
      {% else %}
      <img decoding="async" width="{{ image.width }}" height="{{ image.height }}" sizes="{{ image.sizes }}" {% if image.srcset|length > 0 %}src="{{ image.src }}" srcset="{{ image.srcset }}"{% else %}src="{{ image.src }}"{% endif %} class="{{image.class}}" alt="{{alt}} "/>
      {% endif %}
    </div>
    {% if captions|length > 0 %}
    {% for caption in captions %}
      <figcaption>
        <div class='textbox'>
          {{ caption }}
        </div>
      </figcaption>
    {% endfor %}
    {% endif %}
    {% if htmlAppend %}
      {{ htmlAppend|raw }}
    {% endif %}
    {% if link %}
    </a>
    {% endif %}
  </figure>
{% endif %}
